# 🏆 ToxPredict: Hackathon Winning Playbook

This document serves as your 3-Day Execution Plan, Architecture Overview, and Final Submission Checklist for the Drug Toxicity Prediction Hackathon.

---

## 🏗️ Simplest Winning Architecture

To win a hackathon, your architecture needs to be simple to explain, robust to demo, and technologically sound.

1. **The Data Layer:** Raw SMILES strings → RDKit Validation + Deduplication.
2. **The Feature Engineering Layer:** RDKit (MolWt, LogP, TPSA, etc.) + 1024-bit Morgan Fingerprints. This hybrid approach captures both physical properties and sub-graph structures.
3. **The ML Layer:** XGBoost Classifier. It inherently handles missing values, non-linear relationships, and most importantly, extreme class imbalance (95:5) via `scale_pos_weight`.
4. **The Interpretation Layer:** SHAP (TreeExplainer) to break open the black box and provide mathematical proof of *why* a compound is predicted toxic.
5. **The Presentation Layer:** A responsive, error-proof Streamlit web app that runs locally and handles bad inputs gracefully without crashing during your live demo.

### 📂 Folder Structure
Keep it industry-standard:
```text
drug-toxicity-predictor/
├── data/
│   ├── raw/                        # Original tox21.csv (not committed – download separately)
│   ├── processed/                  # Cleaned features.csv, labels.csv, zinc_demo_sample.csv
│   └── priority_toxins.json        # Offline known-hazard dictionary (bypasses ML)
├── src/
│   ├── __init__.py
│   ├── pipeline_utils.py           # Shared helpers: load model, build features, predict
│   ├── simple_load.py              # Basic dataset inspection
│   ├── inspect_data.py             # Detailed data diagnostics
│   ├── data_loader.py              # Cleans missing/invalid SMILES, merges ChEMBL
│   ├── feature_engineering.py      # Extracts RDKit 2D descriptors + Morgan fingerprints
│   ├── baseline_models.py          # Logistic Regression & Random Forest baselines
│   ├── improve_model.py            # Trains & tunes XGBoost → models/tuned_xgboost_model.pkl
│   ├── compare_chembl_experiment.py# Ablation: with vs. without ChEMBL → models/best_model.pkl
│   ├── evaluate_model.py           # Metric calculation (ROC-AUC / PR-AUC / F1)
│   ├── shap_explain.py             # Generates SHAP summary/waterfall plots
│   ├── explainability.py           # Per-compound SHAP helpers
│   ├── zinc_loader.py              # Loads ZINC-250k demo molecules
│   ├── zinc_screen.py              # Batch-screens 1,000 ZINC molecules
│   ├── zinc_baseline.py            # ZINC baseline screening helper
│   └── final_report.py             # Aggregates all metrics → reports/final_report.md
├── scripts/
│   ├── build_toxin_dictionary.py   # Rebuilds data/priority_toxins.json
│   └── fetch_chembl_withdrawn.py   # Fetches withdrawn drugs from ChEMBL REST API
├── models/
│   ├── tuned_xgboost_model.pkl     # ✅ Pre-trained model committed to repo
│   └── best_model.pkl              # Generated by compare_chembl_experiment.py
├── reports/                        # Saved evaluation plots and metrics JSON files
│   ├── baseline_metrics.json
│   ├── tuned_xgboost_metrics.json
│   ├── chembl_ablation_*.json
│   ├── shap_*.png
│   ├── feature_importance.png
│   ├── zinc_screen_results.csv
│   └── final_report.md
├── notebooks/
│   └── 01_eda.ipynb                # Exploratory data analysis notebook
├── app/
│   └── streamlit_app.py            # Frontend web application
├── tests/
│   └── test_demo.py                # Automated integration tests (pytest)
├── .devcontainer/
│   └── devcontainer.json           # VS Code Dev Container / GitHub Codespaces config
├── README.md                       # Main documentation
├── FOR_BEGINNERS.md                # Beginner-friendly guide (LEGO analogy)
├── HACKATHON_GUIDE.md              # This file
├── DEMO_SCRIPT.md                  # Live demo talking points
└── requirements.txt                # Python dependencies
```

---

## 📅 3-Day Execution Plan (Retrospective)

*If judges ask how you built this, here is your 3-day timeline:*

**Day 1: Data Mastery & Baselines**
*   Explored the Tox21 dataset, identified SMILES and the NR-AR endpoint.
*   Wrote `data_loader.py` to aggressively clean invalid molecules using RDKit.
*   Wrote `baseline_models.py` (Logistic Regression vs Random Forest) to establish a performance floor.

**Day 2: Advanced ML & Explainability**
*   Developed `feature_engineering.py` to extract 1,241 features (descriptors + ECFP4 fingerprints).
*   Upgraded to XGBoost (`improve_model.py`), running GridSearchCV to optimize `max_depth` and `learning_rate` while fiercely penalizing false negatives using `scale_pos_weight`.
*   Integrated SHAP (`shap_explain.py`) to extract the exact structural features driving toxicity.

**Day 3: Productization & Polish**
*   Built `streamlit_app.py`, bridging the raw XGBoost model into a user-friendly frontend.
*   Implemented extreme defensive programming (try-except blocks) so the app survives invalid judge inputs.
*   Added ChEMBL ablation (`compare_chembl_experiment.py`) to compare Tox21-only vs. Tox21+ChEMBL and auto-promote the higher-precision run to `models/best_model.pkl`.
*   Added ZINC-250k virtual screening (`zinc_screen.py`) to demonstrate large-scale batch prediction.
*   Added SHAP explainability plots and a priority toxin bypass dictionary.
*   Polished `README.md` and visualizations for the final pitch.

---

## ✅ Final Verification Checklist (Pre-Submission)

Before finalizing your submission, double-check these critical items:

- [ ] **Code Runs End-to-End:** Ensure a judge could run `pip install -r requirements.txt` followed by `streamlit run app/streamlit_app.py` without fatal crashes. The pre-trained `models/tuned_xgboost_model.pkl` is committed so no re-training is needed.
- [ ] **Dependencies Match:** Check if `requirements.txt` includes: `pandas`, `numpy`, `scikit-learn`, `xgboost`, `rdkit`, `shap`, `streamlit`, `matplotlib`, `seaborn`, `joblib`, `tqdm`, `requests`, `pytest`.
- [x] **No Hardcoded Absolute Paths:** All data loading in `src/` uses `os.path.join(os.path.dirname(__file__), ...)` / `pathlib.Path(__file__).parent` so it works on any computer. *(Verified).*
- [x] **Data Leakage Check:** `StandardScaler` in `improve_model.py` is only `fit()` on the training set. *(Verified: `fit_transform` on train, `transform` on val/test).*
- [x] **App Error Handling:** The Streamlit app shows a friendly error card for invalid SMILES (e.g., "invalid_chemical") instead of a raw Python traceback. *(Verified).*
- [ ] **Live Demo Rehearsal:** Have 2 SMILES ready in your clipboard. One definitively SAFE (`CCO` - Ethanol), and one definitively TOXIC (`O=C(O)CCC(=O)c1ccc(-c2ccccc2)cc1` - sample).
- [x] **Tests Pass:** `pytest tests/` passes all integration checks. *(Run after installing dependencies and ensuring `models/best_model.pkl` exists or skipping that test).*
- [ ] **README Polish:** Ensure your team's names are on the `README.md`.

**Good luck! You've built a technically rigorous, highly interpretable pipeline.**
